import { beardBirth } from '../../../helpers/beard-birth';
import { module, test } from 'qunit';

module('Unit | Helper | beard birth');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = beardBirth(42);
  assert.ok(result);
});
